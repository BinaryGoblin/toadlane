<div class="">
  <div class="col-md-6">
    <div class="devise content">
      <h1 class="header-adjust">Create your economy here.</h1>
      <%=image_tag 'transaction.png', width: "45", class: "pull-left", style: "margin: 20px 15px 20px 0;"%>
      <p class="pull-left"><span class="bold">Safely transact</span> in wholesale volume </p>
      <span class="clearfix"></span>
      <%=image_tag 'notifications.png', width: "45", class: "pull-left", style: "margin: 20px 15px 20px 0;" %>
      <p class="pull-left"><span class="bold">Receive updates</span> on what you buy and sell</p>
      <span class="clearfix"></span>
      <%=image_tag 'verify.png', width: "45", class: "pull-left", style: "margin: 20px 15px 20px 0;" %>
      <p class="pull-left"><span class="bold">Real time verification</span> and same day pay outs</p>
      <span class="clearfix"></span>
      <%=image_tag 'catalogueicon.png', width: "45", class: "pull-left", style: "margin: 20px 15px 20px 0;" %>
      <p class="pull-left"><span class="bold">Upload your entire catalogue</span> in minutes</p>
    </div>
  </div>
  <div class="col-md-6">
    <div class="wrap devise">
      <h1 class="header-adjust mobile">Create your economy here.</h1>
      <h2 class="header-adjust">Sign up now- it’s free.</h2>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div><%= f.hidden_field :terms_accepted_at, :value=> Time.now %></div>
        <br>
        <div><%= f.label :name, 'Name' %>
        <%= f.text_field :name, class: 'form-control' %></div>
        <br>

        <div><%= f.label :email %>
        <%= f.email_field :email, class: 'form-control' %></div>
        <br>

        <div>
          <label><%= f.label :tag_list, value: 'I buy and sell' %></label>
          <%= f.select :tag_list, options_from_collection_for_select(get_all_tags, :name, :name, f.object.tag_list), {}, multiple: true, data: { behavior: 'Select', options: '{"plugins":["remove_button"],"create":"true"}' } %>
        </div>
        <br>

        <div><%= f.label :password %>
        <%= f.password_field :password, autocomplete: "off", class: 'form-control' %></div>
        <br>

        <div class="terms_of_service">
          <%= f.check_box :terms_of_service %> By clicking Sign up, you agree to our <a href="../terms_of_service" target="_blank">Terms of Service</a> and that you have read our Data Policy, including our Cookie Use. You may receive Notifications from Toadlane and can opt out at any time.
        </div>
        <br>

        <div class="btn-signup"><%= f.submit "Sign up", class: 'btn btn-primary' %></div><div class="btn-signin">Already have an account? <%=link_to 'Sign in' , new_user_session_path %></div>
      <% end %>

      <br>
    </div>
  </div>
</div>
