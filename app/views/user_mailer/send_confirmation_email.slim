- if @armor_order.present? && @product.present?
  = link_to 'Confirm my account', set_armor_profile_dashboard_accounts_url(product_id: @product.id, armor_order_id: @armor_order.id, confirmed_email: true)
- else
  = link_to 'Confirm my account', set_armor_profile_dashboard_accounts_url(confirmed_email: true)
